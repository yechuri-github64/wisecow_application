name: CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
    - name: check path
      run: |
        pwd
        ls -al
        cd /
        find . -name "*k8"
        find . -name "*yaml*"
       
    
